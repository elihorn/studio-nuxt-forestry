---
label: Works
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: slug
  type: text
  config:
    required: false
  label: Slug
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: media
  type: image_gallery
  config: {}
  label: Media
