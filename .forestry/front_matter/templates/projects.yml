---
label: Projects
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: slug
  type: text
  config:
    required: false
  label: Slug
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: pages
  type: blocks
  label: Pages
  template_types:
  - media
  - page
  config:
    min: 
    max: 
