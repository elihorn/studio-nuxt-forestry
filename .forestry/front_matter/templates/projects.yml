---
label: Projects
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: slug
  type: text
  config:
    required: false
  label: Slug
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: folder
  type: field_group_list
  fields:
  - name: file
    type: include
    config: {}
    template: media
    label: File
    description: Test
  config:
    min: 
    max: 
    labelField: title
  label: Folder
